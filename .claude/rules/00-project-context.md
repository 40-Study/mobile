# Project context — apply automatically

When writing or modifying code in this project (40Study):

1. **Apply all rules** in this folder (`.cursor/rules/`) before making changes — especially `provider.md` and `test.md`.
2. **Use project skills** from `.cursor/skills/` when the task matches:
   - **bloc** — state management with Bloc/Cubit, events, states, BlocBuilder/BlocProvider, bloc tests.
   - **provider** — state with Provider, ChangeNotifierProvider, Consumer, context.watch/read.
   - **code-review** — when reviewing code or pull requests.

Open and follow the relevant skill(s) and rule(s) at the start of the task so behavior matches this project’s conventions.
